{
  "name": "weather-api",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel src/*",
    "build": "parcel build src/*"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.14.3",
    "typescript": "^3.1.6"
  },
  "dependencies": {
    "normalize.css": "^8.0.0"
  }
}
